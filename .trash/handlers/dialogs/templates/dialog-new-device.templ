package templates

import (
	"github.com/knackwurstking/picow-led/handlers/ids"
	"github.com/knackwurstking/picow-led/utils"
	"github.com/knackwurstking/ui"
	"net/http"
)

templ NewDeviceDialog(oob bool, err error) {
	{{
		hxSwapOOB := ""
		if oob {
			hxSwapOOB = "true"
		}
	}}
	@ui.Dialog(ui.DialogProps{
		ID:               ids.DialogNewDevice,
		Method:           http.MethodPost,
		Href:             utils.HxUrlEditDeviceDialog(nil),
		SubmitButtonText: "Create",
		HxSwapOOB:        hxSwapOOB,
		Error:            err,
	}) {
		@editDeviceDialogContent("", "", "")
	}
}
