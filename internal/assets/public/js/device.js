function changeDeviceColor(addr, hex) {
	// TODO: Change the device color via API call
	console.debug(`Changing color of device ${addr} to`, hex);
}

function changeDeviceWhite(addr, value) {
	// TODO: Change the device color via API call
	console.debug(`Changing white of device ${addr} to`, value);
}

function changeDeviceWhite2(addr, value) {
	// TODO: Change the device color via API call
	console.debug(`Changing white2 of device ${addr} to`, value);
}
